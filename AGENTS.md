# AGENTS.md

本仓库当前第一优先级是：性能、丝滑、稳定可靠。其它核心优先级是：架构先进、API 一致性、动画与过渡满帧、现代化审美、朴素直觉。

## 工作入口

- 本文件是默认工作规则；不要把读取更多文档当成严谨本身
- 不要开局默认读完所有文档；先理解用户任务和相关源码，再按实际需要补读文档
- 只有需要项目概览或脚本时读 `README.md`；需要架构/API/性能原则时读 `docs/project-principles.md`；需要判断包归属或依赖边界时读 `docs/package-boundaries.md`
- 只能看到用户粘贴的上下文时，把本文件视为压缩后的仓库规则；不要默认要求补齐全部文档
- 公共组件重构默认会触碰 API、状态模型、命名、交互和性能路径，必须按本文件的公共组件重构规则执行
- 不要把“优化一下”理解成局部微调；只要现有设计不合理，就应当做最优设计并迁移仓库内调用点
- 不要把用户粘贴的单个文件当作重构边界；公共组件重构范围以公共契约和完整调用链为准
- 缺少相关源码时必须主动要求补充，尤其是组件调用点、导出入口、类型定义、主题 token、尺寸工具、动画工具、Provider / Host / Hook 和相邻组件

## 如何使用这些规则

- 规则是设计边界，不是最终答案；必须保留主动设计、权衡和质疑
- 如果发现规则之间冲突，或某条规则会损害性能、丝滑、稳定可靠，应先指出冲突并给出更合理的方案
- 不要为了机械满足某条局部规则，牺牲整体架构、API 一致性、稳定性、性能、动画满帧或现代化审美
- 不确定时要说明取舍依据，不要假装所有约束天然兼容

## 必须遵守

- 公开入口集中在 `packages/ui/src/index.ts` 与 `packages/tools/src/index.ts`
- 优先修改 `src`，不要手工编辑 `dist`、`Pods`、`build`、`.expo`、`node_modules`
- `packages/ui` 不收业务逻辑；`packages/tools` 不收 UI 逻辑
- 当前阶段允许破坏性重构，不为历史 API 和历史实现保留坏设计
- 性能、丝滑、稳定可靠并列第一，不能为了写法方便、旧 API 或过低系统兼容牺牲这三件事
- 改动优先保证 iOS / Android 稳定性，其次再追求写法“更优雅”
- 需要动画或过渡的地方，目标是全程匹配设备刷新率稳定输出，不默认按 60fps 思考
- 优先采用 React Native、iOS、Android 官方当前推荐的现代实践；不为了兼容过低 Android / iOS 版本把公共实现拖进低性能路径
- API 命名与状态模型优先统一，不继续发散
- 触碰公共组件时，必须先审视公开 props 是否符合当前 API 语言与开发者直觉；如果现有 API 命名、状态模型或配置形态不合理，优先重构 API 并迁移仓库内调用点，而不是在旧 API 下做实现优化
- 外观要符合现代化审美，不做杂乱、廉价、过时的呈现
- UI 层新增或修改的像素尺寸大于 0 的必须通过 `wp(...)` 计算；透明度、比例、时长、权重、zIndex 等非像素语义数值不属于这条约束
- 交互符合用户朴素直觉，API 符合开发者朴素直觉
- 文档保持最小，只更新真正影响代码理解的内容

## 公共组件 API 语言

- 公共组件重构时，默认先审视 props、事件命名、状态模型和默认行为；不合理就破坏性重构
- 同类概念必须同名，优先使用 `value` / `defaultValue`、`checked` / `defaultChecked`、`open` / `defaultOpen`、`onChange`、`disabled`、`loading`、`variant`、`tone`、`size` 或 `sizePreset`
- 有内部状态的组件优先同时支持受控和非受控模式；状态来源必须单一，不能做半受控半非受控
- 事件命名要描述状态变化或用户意图，不用含糊的近义词继续扩散
- 先提供语义化 API，再提供必要 escape hatch；不要让样式碎片化配置反客为主
- 默认值必须符合开发者朴素直觉，简单需求不应要求理解内部机制

## 公共组件重构协议

- 先读目标组件、导出入口、仓库内调用点、相关类型、主题 token、尺寸工具、动画工具和相邻组件
- Provider、Host、Hook、命令式 API、导出入口或调用点会影响最优设计时，必须一并审视，不要只改用户贴出的文件
- 先判断问题属于 API、状态模型、架构边界、稳定性、性能、动画还是视觉交互，再决定改法
- 必须先修根因；不接受只包一层、换个名字、挪几行代码却保留坏设计
- 涉及动画、过渡、手势、高频渲染时，必须优先设计关键帧路径，避免 JS 抖动、布局抖动、桥接开销和不必要重渲染
- 涉及 UI 尺寸时，新增或修改的像素尺寸大于 0 的必须通过 `wp(...)` 计算
- 修改公共 API 时，必须同步迁移仓库内调用点和公开入口，并更新最少必要文档
- 重构后必须重新阅读改后的组件和调用点，主动找 API 反直觉、状态竞争、边界条件、平台差异、性能回退、视觉廉价感和动画掉帧风险
- 如果自检发现更好的设计，应继续改到位，不把明显问题留给下一轮用户指出

## 当前工作方式

- 先判断问题属于架构、API、稳定性还是性能
- 先修根因，不做表面补丁
- 热路径改动要特别谨慎
- 涉及动画、过渡、手势、高频渲染时，先判断是否会引入 JS 抖动、布局抖动、桥接开销或不必要重渲染
- 公共 API 变化时，同步更新最少必要文档
- 完成前必须看一遍 diff，并按公共组件重构协议做一轮自检

## 验证

```bash
pnpm validate
```
